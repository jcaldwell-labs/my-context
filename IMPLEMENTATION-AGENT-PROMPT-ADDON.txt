==================================================
COPY THIS WHEN LAUNCHING IMPLEMENTATION AGENT
==================================================

SUPERVISOR MONITORING PROTOCOL:

You are being monitored by a supervisor process. Follow these signaling protocols for progress visibility:

1. HEARTBEAT (Every 10 minutes):
   - Execute: my-context touch
   - Purpose: Prove you're alive and making progress
   - Supervisor triggers intervention if >30min gap

2. PHASE BOUNDARIES (Required):
   BEFORE starting phase: my-context note "🚀 Starting Phase {N}: {Phase Name} (Tasks {start}-{end})"
   AFTER completing phase: my-context note "✅ Phase {N} complete: {summary} ({completed}/{total} tasks)"

3. CHECKPOINTS (Every 10-15 tasks):
   my-context note "📊 Checkpoint: Tasks {X}-{Y} complete - {what was built}"

4. BLOCKERS (When stuck):
   BLOCKED: my-context note "🚧 BLOCKED: {issue description} - investigating"
   UNBLOCKED: my-context note "✅ UNBLOCKED: {resolution} - continuing"

5. FILE TRACKING (Significant files):
   my-context file "{absolute/path/to/created-file.go}"
   Track files >10 lines or core functionality

6. COMPLETION (When done):
   my-context note "🎉 Implementation complete: All {N} tasks done, tests passing"
   my-context note "✅ Quality gate passed: Implementation complete and validated"
   my-context stop

CONTEXT SIZE: Target 50 notes per phase. If approaching 50, stop current context and start new phase-specific one.

EXAMPLE SIGNALING FLOW:
- my-context note "🚀 Starting Phase 1: Smart Resume (Tasks T001-T017)"
- my-context touch (after 10min of work)
- my-context note "📊 Checkpoint: Tasks T001-T005 complete - Core state methods added"
- my-context file "internal/core/state.go"
- my-context touch (another 10min)
- my-context note "📊 Checkpoint: Tasks T006-T010 complete - Interactive prompts implemented"
- my-context note "✅ Phase 1 complete: Smart resume working, all tests passing (17/17 tasks)"

Supervisor can track your progress in real-time via: my-context show

==================================================
